# Design defaults — template  Copy this to `~/.claude/design-defaults.md` (or `<project>/.claude/design-defaults.md` for per-project overrides) and fill in your preferences. The three-turn-design skill reads this on entry and treats every entry as **binding**, not consultative.  Per-project files override the home file when both exist.  ---  ## Palette discipline  > Pick one. The skill will not ask again.  - [ ] Single brand color only - [ ] Two-color (brand + one accent) — list the accent: `_______` - [ ] Multi-color (brand + 2-3 accents) — list them: `_______` - [ ] Semantic palette (colors map to states like `registered=blue`, `submitted=green`) — list mapping: `_______`  ## Animation budget  > Limits the skill must respect.  - Animation primitives allowed: `CSS keyframes only` | `JS animation loops OK` | `Both` — pick one: `_______` - Memory ceiling for ambient/decorative layers: `_______ MB` - Forbidden CSS properties on animated elements: e.g. `filter: blur()`, `box-shadow` with offsets - Behavior on `prefers-reduced-motion`: `disable all` | `slow down` | `ignore` — pick one: `_______` - Behavior on `visibilitychange` (tab hidden): `pause animations` | `keep running` — pick one: `_______`  ## Server-management cadence  > What the skill must do after every code change.  - [ ] Run type check (`tsc --noEmit` or equivalent) - [ ] Run production build before claiming done - [ ] Kill old server PIDs and restart on the new bundle - [ ] Smoke-verify the served URL responds 200  ## Verification habits  > The skill must do these silently, not ask permission.  - Build > "it compiles" — building is the minimum bar before reporting work as done - Read the actual current file before editing — no assumption-based edits - After dashboard changes, restart the server — don't trust HMR with newly-added files - For backend mutations, hit the running API, not the database directly  ## Banner / popup rule  > Applied to every overlay/banner/modal the skill creates or modifies.  - [ ] Every overlay must have a close (×) button - [ ] Close must persist its dismissal until the underlying state changes - [ ] When confetti / motion is part of an overlay, manual close lets the motion finish; only the card fades  ## State-mutation policy  > What the skill is allowed to write to.  - ALLOWED: API routes (POST/PATCH/DELETE) via curl or fetch with admin auth - FORBIDDEN: direct SQL on app databases (e.g. `sqlite3 app.db UPDATE …`) - FORBIDDEN: editing application state via file system writes outside the app's API - For local dev convenience: same rules apply — reproducibility > shortcuts  ## Grace-notes pass  > The skill's last responsibility before declaring done.  Before Turn 3 ships, the skill checks: - [ ] Transitions on appear / disappear (no abrupt show/hide) - [ ] Motion finishes on user-initiated close (don't cut animation mid-flight) - [ ] Easing curves (no linear default unless intentional) - [ ] Empty states have an aesthetic, not just absence  ## Anti-patterns (forbidden behaviors)  The skill must never: - Take screenshots via `screencapture` of browser UIs (use the visual companion or describe instead) - Edit production data tables directly (use the API) - Skip `--no-verify` flags or other safety-check bypasses - Commit without explicit user instruction - Push to remote without explicit user instruction - Rename or restructure existing files unless the task requires it  ## Project glossary (optional)  > If your project uses domain terms that differ from generic UI vocabulary, list mappings so the skill speaks your language.  | Skill term | Your term | |---|---| | display / viewport | _______ | | participant / user | _______ | | task / event | _______ | | dashboard / admin | _______ |  ## Tangent policy  When mid-session a bug or behavior issue surfaces that is NOT the topic:  - [ ] Park to a one-paragraph note file under `docs/superpowers/specs/YYYY-MM-DD-<topic>-note.md` - [ ] Return the path in the response - [ ] Do NOT derail the current 3-turn arc  ---  ## Last updated  > Update this line whenever you change defaults so the skill can detect staleness.  `updated: YYYY-MM-DD`